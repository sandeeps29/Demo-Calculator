{
  "name": "demo-calculator",
  "version": "1.0.0",
  "description": "",
  "main": "script.js",
  "homepage": "http://sandeeps29.github.io/Demo-Calculator",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf build && mkdir build && cpy index.html build && cpy style.css build && cpy script.js",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cpy-cli": "^5.0.0",
    "gh-pages": "^6.1.1",
    "parcel": "^2.12.0",
    "rimraf": "^5.0.7"
  }
}
